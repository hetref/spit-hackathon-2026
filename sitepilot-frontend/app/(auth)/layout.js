/**
 * Auth Layout Group
 * 
 * Routes in this group:
 * - /auth/signin
 * - /auth/signup
 * - /auth/verify-email
 */

export default function AuthLayout({ children }) {
  return <div>{children}</div>
}
